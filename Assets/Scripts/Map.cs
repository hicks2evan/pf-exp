using UnityEngine;

public class Map : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("I hit a break point ???");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
